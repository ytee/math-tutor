{
  "name": "math-tutor",
  "version": "0.1.0",
  "description": "Interactive math tutor SPA — ClojureScript + Reagent",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "setup": "apt-get install -y openjdk-21-jdk-headless 2>/dev/null; export JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64",
    "dev": "shadow-cljs watch app",
    "build": "shadow-cljs release app",
    "release": "shadow-cljs release app",
    "clean": "rm -rf public/js .shadow-cljs"
  },
  "devDependencies": {
    "shadow-cljs": "2.28.4"
  },
  "dependencies": {
    "katex": "0.16.10"
  }
}